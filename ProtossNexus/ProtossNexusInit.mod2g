use ProtossNexus as knowledge.

module ProtossNexusInit {	
	%initialize resources
	if percept(resources(A,B,C,D)) then insert(resources(A,B,C,D)).
	%initialize queueSize
	if percept(queueSize(S)) then insert(queueSize(S * 2)).
}